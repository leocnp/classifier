# Copy this file to `.env` and fill in your key to enable real (Anthropic) mode.
# Without a key set, the app runs the deterministic mock classifier instead.
# `.env` is git-ignored — never commit your real key.
ANTHROPIC_API_KEY=
